# Dynamic Quantities

A library for quantities which aren't statically defined, but instead are calculated based on runtime data and context.

E.g. `%` -> percentage of one rep max. So this quantity changes as the user's one rep max changes but also which exercise is being performed.

This makes these quantities part of a much higher layer than static quantities.
